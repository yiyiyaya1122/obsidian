---
tags:
  - loss-function
  - machine-learning
  - deep-learning
  - computer-vision
aliases:
  - 损失函数详解
  - 代价函数对比
---
# 1. 什么是损失函数？

**损失函数（Loss Function）** 也称为**代价函数（Cost Function）**，用于度量：

> 👉 **模型预测结果与真实目标之间的差距**

在优化问题中，我们的目标是：

$$  
\min_{\theta}  L(y, f(x;\theta))  
$$

其中：
- $f(x;\theta)$：模型
- $y$：真实标签
- $L(\cdot)$：损失函数

**数学定义**：
$$
L(\theta) = \frac{1}{N} \sum_{i=1}^N \ell(y_i, f(x_i; \theta))
$$

> **优化算法负责"怎么走"，损失函数决定"往哪走"。**

## 损失函数的作用
1. **衡量性能**：量化模型好坏
2. **指导优化**：为优化器提供梯度方向
3. **防止过拟合**：结合正则化项
4. **多目标平衡**：在多任务学习中平衡不同目标

## 损失函数的要求
- **可微性**：便于梯度下降优化
- **凸性**：理想情况下是凸函数，保证全局最优
- **敏感性**：能敏感反映预测误差
- **稳定性**：数值计算稳定

---

# 2. 损失函数的几何与统计本质

## 2.1 几何视角
- 损失函数定义了 **参数空间中的能量地形（Energy Landscape）**
- 梯度下降在该地形上寻找最低点

不同损失函数 ⇒ 不同曲面形状 ⇒ 不同收敛行为

## 2.2 统计视角（极大似然）
多数常见损失函数可由 **极大似然估计（MLE）** 推导而来：

| 假设噪声分布 | 对应损失函数 |
|-------------|-------------|
| 高斯分布 | MSE / L2 |
| 拉普拉斯分布 | MAE / L1 |
| 伯努利分布 | Binary Cross Entropy |
| 多项分布 | Categorical Cross Entropy |

> 📌 **选择损失函数，本质是在选择数据噪声模型。**

---

# 3. 回归问题损失函数

## 3.1 均方误差（MSE / L2 Loss）
$$  
L_{\text{MSE}} = \frac{1}{N} \sum_{i=1}^N (y_i-\hat y_i)^2  
$$

**梯度**：
$$
\frac{\partial L}{\partial \hat{y}_i} = -2(y_i - \hat{y}_i)
$$

**特点**：
- 光滑、可导
- 对异常值非常敏感（二次增长）

**典型应用**：
- 线性回归
- 深度回归任务（假设误差正态分布）

---

## 3.2 平均绝对误差（MAE / L1 Loss）
$$  
L_{\text{MAE}} = \frac{1}{N} \sum_{i=1}^N |y_i-\hat y_i|  
$$

**梯度**：
$$
\frac{\partial L}{\partial \hat{y}_i} = \text{sign}(y_i - \hat{y}_i)
$$

**特点**：
- 对异常值鲁棒（线性增长）
- 在0处不可导（使用次梯度）

---

## 3.3 Huber Loss（平滑 L1）
$$  
L_\delta =  
\begin{cases}  
\frac{1}{2}(y-\hat y)^2 & |y-\hat y| \le \delta \\  
\delta(|y-\hat y|-\frac{1}{2}\delta) & \text{otherwise}  
\end{cases}  
$$

**特点**：
- 小误差用 L2（可导）
- 大误差用 L1（鲁棒）

**典型应用**：
- Faster R-CNN
- 目标检测回归分支

---

## 3.4 Log-Cosh损失
$$  
L = \frac{1}{N} \sum_{i=1}^N \log(\cosh(y_i - \hat{y}_i))
$$

**近似特性**：
- 当误差小时 ≈ $\frac{1}{2}(y-\hat{y})^2$
- 当误差大时 ≈ $|y-\hat{y}| - \log(2)$

**优点**：
- 处处二次可导
- 比Huber损失更平滑

---

# 4. 分类问题损失函数

## 4.1 0-1 Loss（理论损失）
$$  
L_{0-1} = \mathbb{I}(y \ne \hat y)  
$$
- 不可导
- 仅用于理论分析

---

## 4.2 二分类交叉熵（Binary Cross Entropy）
$$  
L = -[y\log p + (1-y)\log(1-p)]  
$$
其中：  
$$  
p = \sigma(z)  
$$

**本质**：伯努利分布的负对数似然。

**特点**：
- 分类任务最常用
- 对错误分类惩罚大

---

## 4.3 多分类交叉熵（Softmax + CE）
$$  
L = - \sum_{k=1}^K y_k \log p_k  
$$
$$  
p_k = \frac{e^{z_k}}{\sum_j e^{z_j}}  
$$

**工程技巧**：
- Log-Softmax + NLLLoss（数值稳定性更好）

---

## 4.4 Label Smoothing
$$  
y_k =  
\begin{cases}  
1-\varepsilon & k=y \\  
\varepsilon/(K-1) & k\ne y  
\end{cases}  
$$

**作用**：
- 抑制过拟合
- 提升泛化能力

---

## 4.5 合页损失（Hinge Loss）
$$  
L = \frac{1}{N} \sum_{i=1}^N \max(0, 1 - y_i \cdot f(x_i))
$$
其中 $y_i \in \{-1, +1\}$。

**适用场景**：
- 支持向量机（SVM）
- 需要最大间隔分类

---

## 4.6 指数损失（Exponential Loss）
$$  
L = \frac{1}{N} \sum_{i=1}^N \exp(-y_i \cdot f(x_i))
$$
**适用场景**：
- AdaBoost算法
- 集成学习

---

# 5. 度量学习与相似度损失

## 5.1 Contrastive Loss
$$  
L = y d^2 + (1-y) \max(0, m-d)^2  
$$

## 5.2 Triplet Loss
$$  
L = \max(0, d(a,p)-d(a,n)+m)  
$$
**应用**：
- 人脸识别
- 行人重识别（ReID）

## 5.3 Cosine / InfoNCE Loss
$$  
L = -\log \frac{\exp(\text{sim}(q,k^+)/\tau)}{\sum_i \exp(\text{sim}(q,k_i)/\tau)}  
$$
**应用**：
- CLIP
- 自监督学习

---

# 6. 目标检测与分割常用损失

## 6.1 Focal Loss
$$  
L = -(1-p_t)^\gamma \log(p_t)  
$$
其中 $p_t = \begin{cases} p & \text{if } y=1 \\ 1-p & \text{otherwise} \end{cases}$

**解决**：类别不平衡问题

## 6.2 IoU 系列损失
- **IoU Loss**：直接优化交并比
- **GIoU Loss**：考虑非重叠区域
- **DIoU Loss**：考虑中心点距离
- **CIoU Loss**：考虑长宽比

**特点**：直接优化几何重叠关系

## 6.3 Dice Loss
$$  
\text{Dice} = \frac{2|A\cap B|}{|A|+|B|}  
$$
$$  
L_{\text{Dice}} = 1 - \text{Dice}  
$$
**典型应用**：医学图像分割

---

# 7. 强化学习中的损失函数

## 7.1 Value Loss
$$  
L_V = (V(s)-R)^2  
$$

## 7.2 Policy Gradient Loss
$$  
L_\pi = -\mathbb{E}[\log \pi(a|s) A]  
$$

## 7.3 PPO Clipped Loss
$$  
L = -\min(r_t A, \text{clip}(r_t,1-\epsilon,1+\epsilon)A)  
$$

---

# 8. 高级损失函数技术

## 8.1 正则化损失
**L1正则化（Lasso）**：
$$
L_{\text{total}} = L_{\text{data}} + \lambda \sum |\theta_i|
$$
特点：产生稀疏解，特征选择

**L2正则化（Ridge）**：
$$
L_{\text{total}} = L_{\text{data}} + \lambda \sum \theta_i^2
$$
特点：防止过拟合，权重衰减

**弹性网（Elastic Net）**：
$$
L_{\text{total}} = L_{\text{data}} + \lambda_1 \sum |\theta_i| + \lambda_2 \sum \theta_i^2
$$
特点：结合L1和L2优点

## 8.2 不平衡数据损失
**加权交叉熵**：
$$
L = -\frac{1}{N} \sum_{i=1}^N [w_{pos} y_i \log(p_i) + w_{neg} (1-y_i) \log(1-p_i)]
$$
其中 $w_{pos} = \frac{N_{neg}}{N_{pos} + N_{neg}}$, $w_{neg} = \frac{N_{pos}}{N_{pos} + N_{neg}}$

## 8.3 多任务学习损失
**加权求和**：
$$
L = \sum_{k=1}^K w_k L_k
$$

**不确定性加权**：
$$
L = \sum_{k=1}^K \frac{1}{2\sigma_k^2} L_k + \log \sigma_k
$$
其中 $\sigma_k$ 是可学习的不确定性参数

---

# 9. 损失函数手动计算示例

## 问题设定

**训练数据**：

| $i$ | $x$ | $y_{\text{真实}}$ | $\hat{y}_{\text{预测}}$ | $p_{\text{预测}}$ |
|-----|-----|-------------------|-------------------------|-------------------|
| 1   | 1.0 | 1.0              | 1.2                    | 0.8              |
| 2   | 2.0 | 0.0              | 0.3                    | 0.3              |
| 3   | 3.0 | 1.0              | 0.7                    | 0.6              |
**说明**：
- 回归任务：$y_{\text{真实}}$ 是连续值
- 二分类任务：$y_{\text{真实}} \in \{0,1\}$，$p_{\text{预测}}$ 是概率

## 9.1 均方误差（MSE）计算
$$
\begin{aligned}
L_{\text{MSE}} &= \frac{1}{3} \left[ (1.0-1.2)^2 + (0.0-0.3)^2 + (1.0-0.7)^2 \right] \\
&= \frac{1}{3} \left[ (-0.2)^2 + (-0.3)^2 + (0.3)^2 \right] \\
&= \frac{1}{3} \left[ 0.04 + 0.09 + 0.09 \right] \\
&= \frac{1}{3} \times 0.22 = 0.0733
\end{aligned}
$$

**梯度计算**（对 $\hat{y}_1 = 1.2$）：
$$
\frac{\partial L}{\partial \hat{y}_1} = -2(1.0 - 1.2) = 0.4
$$

## 9.2 平均绝对误差（MAE）计算
$$
\begin{aligned}
L_{\text{MAE}} &= \frac{1}{3} \left[ |1.0-1.2| + |0.0-0.3| + |1.0-0.7| \right] \\
&= \frac{1}{3} \left[ 0.2 + 0.3 + 0.3 \right] \\
&= \frac{1}{3} \times 0.8 = 0.2667
\end{aligned}
$$

**梯度计算**（对 $\hat{y}_1 = 1.2$）：
$$
\frac{\partial L}{\partial \hat{y}_1} = \text{sign}(1.0 - 1.2) = -1
$$

## 9.3 Huber损失计算（$\delta = 0.5$）

**样本1**：$|1.0-1.2| = 0.2 \leq 0.5$，使用MSE部分：
$$
\ell_1 = \frac{1}{2}(0.2)^2 = 0.02
$$

**样本2**：$|0.0-0.3| = 0.3 \leq 0.5$：
$$
\ell_2 = \frac{1}{2}(0.3)^2 = 0.045
$$

**样本3**：$|1.0-0.7| = 0.3 \leq 0.5$：
$$
\ell_3 = \frac{1}{2}(0.3)^2 = 0.045
$$

**总损失**：
$$
L_{\text{Huber}} = \frac{1}{3}(0.02 + 0.045 + 0.045) = 0.0367
$$

## 9.4 二分类交叉熵损失计算

**样本1**：$y=1, p=0.8$
$$
\ell_1 = -[1 \times \log(0.8) + 0 \times \log(0.2)] = -\log(0.8) = 0.2231
$$

**样本2**：$y=0, p=0.3$
$$
\ell_2 = -[0 \times \log(0.3) + 1 \times \log(0.7)] = -\log(0.7) = 0.3567
$$

**样本3**：$y=1, p=0.6$
$$
\ell_3 = -[1 \times \log(0.6) + 0 \times \log(0.4)] = -\log(0.6) = 0.5108
$$

**总损失**：
$$
L_{\text{CE}} = \frac{1}{3}(0.2231 + 0.3567 + 0.5108) = 0.3635
$$

---

# 10. 损失函数对比分析

## 回归损失函数对比表
| 损失函数 | 样本1 $(y=1.0, \hat{y}=1.2)$ | 样本2 $(y=0.0, \hat{y}=0.3)$ | 样本3 $(y=1.0, \hat{y}=0.7)$ | 总损失 | 特点 |
|----------|------------------------------|------------------------------|------------------------------|--------|------|
| **MSE** | 0.04 | 0.09 | 0.09 | 0.0733 | 对大误差惩罚重 |
| **MAE** | 0.2 | 0.3 | 0.3 | 0.2667 | 对异常值鲁棒 |
| **Huber** ($\delta=0.5$) | 0.02 | 0.045 | 0.045 | 0.0367 | 平衡鲁棒性与可导性 |

## 梯度特性对比
| 损失函数 | 梯度公式 | 梯度特点 |
|----------|----------|----------|
| **MSE** | $\nabla = -2(y-\hat{y})$ | 线性，随误差增大而增大 |
| **MAE** | $\nabla = \text{sign}(y-\hat{y})$ | 恒定幅度，方向不变 |
| **Huber** | $\nabla = \begin{cases} -(y-\hat{y}) & |y-\hat{y}| \leq \delta \\ -\delta\cdot\text{sign}(y-\hat{y}) & \text{otherwise} \end{cases}$ | 小误差时线性，大误差时恒定 |
| **交叉熵** | $\nabla = p - y$（对logits） | 与概率误差成正比 |

---

# 11. 损失函数选择指南

| 任务 | 推荐损失函数 | 理由 |
|------|-------------|------|
| **回归** | MSE / Huber | MSE假设高斯噪声，Huber更鲁棒 |
| **二分类** | BCE | 标准选择，概率解释 |
| **多分类** | Softmax + CE | 标准选择 |
| **目标检测** | Focal + IoU | 处理类别不平衡，优化定位 |
| **语义分割** | Dice + BCE | 处理类别不平衡，优化重叠 |
| **对比学习** | InfoNCE | 学习表征相似度 |
| **强化学习** | PG / PPO Loss | 策略梯度优化 |

## 根据数据特点选择
| 数据特点 | 推荐损失函数 | 理由 |
|----------|--------------|------|
| **无异常值，误差正态分布** | **MSE** | 最优估计，计算简单 |
| **有异常值** | **MAE** 或 **Huber** | 对异常值不敏感 |
| **需要处处可导** | **Huber** 或 **Log-Cosh** | 优化更稳定 |
| **类别不平衡** | **加权交叉熵** 或 **Focal Loss** | 关注难例 |

---

# 12. 实现示例

## PyTorch 实现
```python
import torch
import torch.nn as nn
import torch.nn.functional as F

# 基础损失函数
mse_loss = nn.MSELoss()          # 均方误差
mae_loss = nn.L1Loss()           # 平均绝对误差
ce_loss = nn.CrossEntropyLoss()  # 交叉熵损失
bce_loss = nn.BCELoss()          # 二元交叉熵
huber_loss = nn.SmoothL1Loss(beta=1.0)  # Huber损失

# Focal Loss实现
class FocalLoss(nn.Module):
    def __init__(self, alpha=0.25, gamma=2.0):
        super().__init__()
        self.alpha = alpha
        self.gamma = gamma
        
    def forward(self, inputs, targets):
        BCE_loss = F.binary_cross_entropy_with_logits(inputs, targets, reduction='none')
        pt = torch.exp(-BCE_loss)
        F_loss = self.alpha * (1-pt)**self.gamma * BCE_loss
        return F_loss.mean()